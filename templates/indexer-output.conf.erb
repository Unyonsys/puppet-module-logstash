output {
  # enable for debug only
  # stdout { }

  # If your elasticsearch server is discoverable with multicast, use this:
  #elasticsearch { }

  # If you can't discover using multicast, set the address explicitly
  elasticsearch {
<% if scope.lookupvar('logstash::es_embedded') %>
    embedded => true
<% else %>
    host => "<%=scope.lookupvar('logstash::es_host')%>"
<% end %>
  }
}
